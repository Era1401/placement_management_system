# placement_management_system

## Placement dashboard

The dashboard is served by Spring Boot from `src/main/resources/static`.
Start the application and open [http://localhost:8080/](http://localhost:8080/).
It loads summary and activity data from the `/api` endpoints in the same application.
